const discord = require("discord.js")
let tickets = require("./../../server.js")
const ticket = new tickets()
module.exports = {
  name: "rename",
  aliases: [],
  description: "Add someone to the ticket",
  run: async (client, message, args) => {
    let chan = ticket.renameTicket({
      message: message,  //The way you defined message in the message event
      name: args[0] //The name you will rename it to    
    })
    
    message.channel.send(`${message.author} have renamed the ticket name to ${args[0]}`)
  }
}